# Assets Folder

Place your hero background image here as `hero-bg.jpg` for the dashboard background.

You can download a maritime/ship background image and save it as:

- `hero-bg.jpg` (recommended size: 1920x1080 or larger)

The app will still work without the image, it will just show a dark background with overlay.
