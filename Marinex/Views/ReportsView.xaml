<Window x:Class="Marinex.Views.ReportsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reports - Marinex" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="BgDark" Color="#1E1E2E"/>
        <SolidColorBrush x:Key="BgCard" Color="#313244"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#CDD6F4"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A6ADC8"/>
        <SolidColorBrush x:Key="AccentColor" Color="#89B4FA"/>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#181825"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterRadioButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="15" 
                                Padding="15,6"
                                BorderBrush="#45475A"
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="#1E1E2E"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BgCard}" Padding="30,20" BorderBrush="#45475A" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="📊 Reports Dashboard" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              Foreground="White"/>
                    <TextBlock Text="View and manage all reports" 
                              FontSize="13" 
                              Foreground="{StaticResource TextSecondary}" 
                              Margin="0,5,0,0"/>
                </StackPanel>
                
                <Button Content="➕ New Report" 
                       x:Name="btnNewReport"
                       Click="btnNewReport_Click"
                       HorizontalAlignment="Right"
                       Width="140" 
                       Height="40"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Background="#F9E2AF"
                       Foreground="#1E1E2E"
                       BorderThickness="0"
                       Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Reports List -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search -->
                <TextBox Grid.Row="0" 
                         x:Name="txtSearch" 
                         TextChanged="txtSearch_TextChanged"
                         Margin="0,0,0,15">
                    <TextBox.Resources>
                        <VisualBrush x:Key="HintText" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock Text="🔍 Search reports..." Foreground="{StaticResource TextSecondary}"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </TextBox.Resources>
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="{x:Null}">
                                    <Setter Property="Background" Value="{StaticResource HintText}"/>
                                </Trigger>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="{StaticResource HintText}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Filter Tabs -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                    <RadioButton x:Name="rbAll" Content="All" IsChecked="True" Checked="FilterChanged" Style="{StaticResource FilterRadioButton}"/>
                    <RadioButton x:Name="rbPollution" Content="🗑️ Pollution" Checked="FilterChanged" Style="{StaticResource FilterRadioButton}"/>
                    <RadioButton x:Name="rbSafety" Content="⚠️ Safety" Checked="FilterChanged" Style="{StaticResource FilterRadioButton}"/>
                    <RadioButton x:Name="rbMaintenance" Content="🔧 Maint." Checked="FilterChanged" Style="{StaticResource FilterRadioButton}"/>
                </StackPanel>

                <!-- List -->
                <Border Grid.Row="2" Background="{StaticResource BgCard}" CornerRadius="12" Padding="10">
                    <ListBox x:Name="lstReports"
                            SelectionChanged="lstReports_SelectionChanged"
                            BorderThickness="0"
                            Background="Transparent"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Margin" Value="0,0,0,5"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#45475A"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#585B70"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" 
                                              FontWeight="Bold" 
                                              FontSize="14"
                                              Foreground="White"
                                              TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Summary}" 
                                              FontSize="12" 
                                              Foreground="{StaticResource TextSecondary}"
                                              TextWrapping="Wrap"
                                              Margin="0,3,0,5"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Border Background="#1E1E2E" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding Type}" FontSize="10" Foreground="{StaticResource AccentColor}" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Text="{Binding DateString}" FontSize="11" Foreground="#6C7086" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Grid>

            <!-- Report Detail -->
            <Border Grid.Column="2" 
                   Background="{StaticResource BgCard}" 
                   CornerRadius="15" 
                   Padding="30">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="pnlReportDetail">
                        <!-- Empty state -->
                        <StackPanel x:Name="pnlEmptyState" 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center"
                                   Margin="0,150,0,0">
                            <TextBlock Text="📋" 
                                      FontSize="60" 
                                      HorizontalAlignment="Center" 
                                      Foreground="#45475A"/>
                            <TextBlock Text="Select a report to view details" 
                                      FontSize="16" 
                                      Foreground="{StaticResource TextSecondary}" 
                                      HorizontalAlignment="Center"
                                      Margin="0,15,0,0"/>
                        </StackPanel>

                        <!-- Report detail will be populated dynamically -->
                        <TextBlock x:Name="txtReportDetail" 
                                  FontFamily="Consolas"
                                  FontSize="14"
                                  Foreground="#CDD6F4"
                                  TextWrapping="Wrap"
                                  LineHeight="22"
                                  Visibility="Collapsed"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>