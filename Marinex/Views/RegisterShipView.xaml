<Window x:Class="Marinex.Views.RegisterShipView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Register New Ship" 
        Height="700" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="BgDark" Color="#1E1E2E"/>
        <SolidColorBrush x:Key="BgCard" Color="#313244"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#CDD6F4"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A6ADC8"/>
        <SolidColorBrush x:Key="AccentColor" Color="#89B4FA"/>
        <SolidColorBrush x:Key="InputBg" Color="#181825"/>

        <Style x:Key="InputLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="InputTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/> <!-- Ensure text is visible -->
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                         Template="{DynamicResource ComboBoxToggleButton}" 
                                         Grid.Column="2" 
                                         Focusable="false"
                                         IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                         ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="15,0,23,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left" />
                            <TextBox x:Name="PART_EditableTextBox"
                                    Style="{x:Null}" 
                                    Template="{DynamicResource ComboBoxTextBox}" 
                                    HorizontalAlignment="Left" 
                                    VerticalAlignment="Center" 
                                    Margin="3,3,23,3"
                                    Focusable="True" 
                                    Background="{StaticResource InputBg}"
                                    Foreground="White"
                                    Visibility="Hidden"
                                    IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"                
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{StaticResource BgCard}"
                                            BorderThickness="1"
                                            BorderBrush="#45475A"
                                            CornerRadius="8"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" 
                        Grid.ColumnSpan="2"
                        CornerRadius="8"
                        Background="{StaticResource InputBg}"
                        BorderBrush="#45475A"
                        BorderThickness="1" />
                <Path x:Name="Arrow" 
                      Grid.Column="1" 
                      Fill="{StaticResource TextSecondary}"
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Center"
                      Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z" />
            </Grid>
        </ControlTemplate>
        
        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
        </ControlTemplate>

        <Style x:Key="SubmitButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="#1E1E2E"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                    <TextBlock Text="🚢" FontSize="28" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Register New Vessel" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   Foreground="White"/>
                        <TextBlock Text="Add ship to monitoring system" 
                                   FontSize="13" 
                                   Foreground="{StaticResource TextSecondary}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Ship Name -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Ship Name" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtShipName" Style="{StaticResource InputTextBox}" />
                </StackPanel>

                <!-- Ship Type -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Ship Type" Style="{StaticResource InputLabel}"/>
                    <ComboBox x:Name="cmbShipType" Style="{StaticResource InputComboBox}">
                        <ComboBoxItem Content="Container Ship" IsSelected="True"/>
                        <ComboBoxItem Content="Oil Tanker"/>
                        <ComboBoxItem Content="Bulk Carrier"/>
                        <ComboBoxItem Content="Fishing Vessel"/>
                        <ComboBoxItem Content="Passenger Ship"/>
                        <ComboBoxItem Content="Tugboat"/>
                        <ComboBoxItem Content="Other"/>
                    </ComboBox>
                </StackPanel>

                <!-- Owner -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Owner / Company" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtOwner" Style="{StaticResource InputTextBox}" />
                </StackPanel>

                <!-- MMSI -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="MMSI (9 Digits)" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtMMSI" Style="{StaticResource InputTextBox}" MaxLength="9" />
                    <TextBlock Text="Required for AIS Tracking" FontSize="11" Foreground="#585B70" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Capacity -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Capacity (Tons)" Style="{StaticResource InputLabel}"/>
                    <TextBox x:Name="txtCapacity" Style="{StaticResource InputTextBox}" />
                </StackPanel>

                <!-- Status -->
                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Current Status" Style="{StaticResource InputLabel}"/>
                    <ComboBox x:Name="cmbStatus" Style="{StaticResource InputComboBox}">
                        <ComboBoxItem Content="Docked" IsSelected="True"/>
                        <ComboBoxItem Content="Sailing"/>
                        <ComboBoxItem Content="Maintenance"/>
                        <ComboBoxItem Content="Anchored"/>
                    </ComboBox>
                </StackPanel>

                <!-- AIS Enabled Checkbox -->
                <CheckBox x:Name="chkAISEnabled" Content="Enable AIS Tracking" IsChecked="True" Margin="0,5,0,25" FontSize="14" Foreground="{StaticResource TextPrimary}"/>

                <!-- Buttons -->
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Content="Cancel" 
                            Click="btnCancel_Click"
                            Background="Transparent"
                            Foreground="{StaticResource TextSecondary}"
                            FontSize="14"
                            Cursor="Hand"
                            HorizontalAlignment="Left"
                            BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="Transparent">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <Button Grid.Column="1" 
                            Content="Register Ship" 
                            Click="btnRegister_Click"
                            Style="{StaticResource SubmitButton}"
                            Width="180"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>